# Rust build output.
/target/
# Node modules for the dashboard source.
web/node_modules/
# web/dist is NOT ignored: the built dashboard assets are embedded into
# the binary with rust-embed (SPEC 14.1).
/bentod-bin
.worktrees
